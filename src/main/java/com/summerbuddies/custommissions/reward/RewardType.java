package com.summerbuddies.custommissions.reward;

/** The kinds of reward (and on-accept/on-complete hook) the mission DSL supports. */
public enum RewardType {
    ITEM,
    XP,
    COMMAND,
    CUTSCENE,
    UNLOCK_GATE,
    COMPANION,
    LORE_STAGE_ADVANCE
}
